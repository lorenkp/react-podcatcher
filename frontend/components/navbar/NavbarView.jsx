import React from 'react';
import SearchView from '../search/SearchView';

const NavbarView = React.createClass({
  render: function() {
    return (
      <SearchView />
      )
  }
});

module.exports = NavbarView;